<%= form_for(@group_shot) do |f| %>
  <% if @group_shot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group_shot.errors.count, "error") %> prohibited this group_shot from being saved:</h2>

      <ul>
      <% @group_shot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
